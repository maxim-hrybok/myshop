This is an MVC web application written in native PHP using RESTful routing (FastRoute), the Smarty template engine, and PDO for database operations.

!!ADD temlates_c and cashe folders !! + add .env with 
DB_HOST=your host
DB_NAME=
DB_USER=
DB_PASS=
DB_CHARSET=utf8mb4
